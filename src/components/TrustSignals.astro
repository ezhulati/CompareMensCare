---
import { Search, Check } from 'lucide-astro';

interface Props {
  lastUpdated?: Date;
  reviewsCount?: number;
  servicesCompared?: number;
}

const {
  lastUpdated = new Date(),
  reviewsCount = 10000,
  servicesCompared = 4,
} = Astro.props;

const formattedDate = lastUpdated.toLocaleDateString('en-US', {
  month: 'long',
  year: 'numeric',
});
---

<section class="py-12 bg-blue-50/30">
  <div class="container-custom max-w-4xl">
    <div class="trust-callout px-4">
  <div class="trust-callout-header">
    <Search class="trust-callout-icon" />
    <span>How We Compared These Services</span>
  </div>

  <div class="trust-callout-body">
    Our team researched, tested, and analyzed these services to bring you unbiased comparisons.
  </div>

  <ul class="trust-callout-list">
    <li class="trust-callout-item">
      <Check class="trust-callout-check" />
      <span>Aggregated <strong>{reviewsCount.toLocaleString()}+</strong> user reviews from multiple sources</span>
    </li>
    <li class="trust-callout-item">
      <Check class="trust-callout-check" />
      <span>Verified current pricing and features directly with providers</span>
    </li>
    <li class="trust-callout-item">
      <Check class="trust-callout-check" />
      <span>Tested signup and consultation processes ourselves</span>
    </li>
    <li class="trust-callout-item">
      <Check class="trust-callout-check" />
      <span>Last updated: <strong>{formattedDate}</strong></span>
    </li>
  </ul>

  <div class="mt-4 pt-4 border-t border-blue-200">
    <p class="text-sm text-blue-700">
      <a href="/methodology" class="trust-callout-link">
        Read our full methodology →
      </a>
    </p>
  </div>
    </div>
  </div>
</section>
